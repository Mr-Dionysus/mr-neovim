--                            ╭─────────────────────╮
--                            │ Git Merge Conflicts │
--                            ╰─────────────────────╯
-- A plugin to visualise and resolve merge conflicts in Neovim.

return {
  'akinsho/git-conflict.nvim',
  version = '*',
  config = true,
}
